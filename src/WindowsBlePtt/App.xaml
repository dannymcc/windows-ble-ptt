<Application x:Class="WindowsBlePtt.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:WindowsBlePtt.Converters"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <conv:BoolToVisibilityConverter x:Key="BoolToVisible"/>
            <conv:InvertBoolConverter x:Key="Invert"/>
            <!-- VoxDMR-matched palette so the PoC reads as a sibling to the Android PoC. -->
            <SolidColorBrush x:Key="Background" Color="#0E0E10"/>
            <SolidColorBrush x:Key="Surface" Color="#1B1B1E"/>
            <SolidColorBrush x:Key="SurfaceElevated" Color="#26262B"/>
            <SolidColorBrush x:Key="Outline" Color="#2E2E33"/>
            <SolidColorBrush x:Key="OnBackground" Color="#E6E6E8"/>
            <SolidColorBrush x:Key="Muted" Color="#8E8E93"/>
            <SolidColorBrush x:Key="Coral" Color="#EF5A4D"/>
            <SolidColorBrush x:Key="PillBlue" Color="#B1CEEF"/>

            <Style x:Key="SectionLabel" TargetType="TextBlock">
                <Setter Property="Foreground" Value="{StaticResource Muted}"/>
                <Setter Property="FontSize" Value="11"/>
                <Setter Property="FontWeight" Value="Medium"/>
                <Setter Property="Margin" Value="0,18,0,8"/>
                <Setter Property="Text" Value=""/>
            </Style>

            <Style TargetType="Button" x:Key="PillButton">
                <Setter Property="Background" Value="{StaticResource PillBlue}"/>
                <Setter Property="Foreground" Value="{StaticResource Background}"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Padding" Value="18,10"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border CornerRadius="22"
                                    Background="{TemplateBinding Background}"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
